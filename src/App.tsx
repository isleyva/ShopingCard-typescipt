import { Routes, Route } from 'react-router-dom'
import {Container} from "react-bootstrap"



function App() {
  

  return <Container>
    <Routes>
      < Route path="/" element={}/>
      < Route path="/" element={}/>
      < Route path="/" element={}/>
    </Routes>
  </Container>
}

export default App
